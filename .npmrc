legacy-peer-deps=true

# Hace que npm instale optional dependencies para TODAS las plataformas.
# Permite que el package-lock.json generado en Windows incluya
# los binarios nativos de Linux necesarios en CI (Vercel/Cloudflare).
ignore-platform=true

# Asegura que npm NUNCA omita optional dependencies en ningún entorno.
include=optional
